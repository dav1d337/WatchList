package com.example.myapplication.utils

import androidx.fragment.app.Fragment
import com.example.myapplication.di.AppComponent
import com.example.myapplication.di.ApplicationModule

/*
fun <T : Fragment> T.mmtravelComponent() : AppComponent {
    val component = (this.activity!!.application as App).applicationComponent
        .newApplicationComponent(ApplicationModule(activity))

    return component
}*/
